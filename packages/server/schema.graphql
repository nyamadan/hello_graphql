scalar DateTime
scalar TodoStatus

type Query {
  getTodo(id: String!): Todo
  getTodoList: [Todo!]!
}

type Mutation {
  addTodo(text: String!): Todo
}

type Todo {
  id: String!
  text: String!
  status: TodoStatus!
  createdAt: DateTime!
}
